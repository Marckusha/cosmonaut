Игра
+ Эффект после нажатия стоит сделать полупрозранчым, и раньше уводить. После одного нажатия исходит три волны, это смущает
- Стоит поработать над точностью столкновений. В узких местах визуально кажется, что персонаж проходит, но получает повреждения. Возможно, нужно два bounding boxa - один для головы, который будет принимать повреждения
+ Сила удара зависит от скорости движения персонажа? Если нет, то можно добавить.

Код
- В настройках зависимостей нужно указывать относительные пути. У меня игра не собралась без правок проекта
D:\Studies\Playrix\folders\cosmonaut\libs\box2d
C:\Users\markov-k\Downloads\cosmonaut\libs\box2d

+ const char* nameText Лучше везде использовать std::string
+ Node::GetShape возвращает указатель на объект, выделенный с помощью new. Нигде не нашел удаление. Этим занимается box2d?
+ Node обязательно быть абстрактным базовым классом?
+ inline const FPoint GetPosition() const. Мелочь, но бессмысленно возвращать const FPoint
+ const float GetConstantsByKey(const std::string& key) const;
    + Зачем возвращать const float?
    + Что будет, если key не будет найден?
    + Нужно добавить возможность для вызывающего кода указать значение по умолчанию, если key не найден

- ContactListener::BeginContact. На будущее: логику обработки столкновений лучше вынести в объекты: игрок, стена, мусор, бонус. Если типов объектов станет больше, этот метод разрастется
+ Не нашел использования наследников DynamicObjectFactory. Еще не реализовано?
+ Возможно, лучше не наследовать от DynamicObjectFactory, а сделать методы: CreateDebris, CreateBonus, CreatePlayer. Или CreateDynamicObject(ObjectType type, ...)
+ _world->Step(timeStep, 6, 2); апдейт игрового мира не будет зависить от dt?
+ Лучше инициализировать поля в объявлении класса, чтобы избежать дублирования в перегруженных конструкторах
+ b2World передается аргументом. Посмотри в сторону Singleton
+ Вместо atoi лучше использовать utils::lexical_cast
- Player всю архитектуру пересмотреть
- Player::Damage - если _health == 79, ни одна ветка условия не выполнится